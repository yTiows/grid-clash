# Grid Clash — Handoff Brief for Claude Code

Paste this whole file as your first message, or say: "Read CLAUDE_CODE_BRIEF.md and execute it end to end." Everything below is real context from the person who owns this project, not a template.

---

## 0. Read this before touching anything

This codebase has already been damaged once by an agent (a previous chat-based Claude session) that built a second, parallel, worse version of a system that already existed, because it didn't check the filesystem first. It also inherited an earlier "layer" of dead/broken code from a prior pass that looked plausible but referenced tables and columns that don't exist. Both were found only by actually running `tsc --noEmit` and reading migrations end to end, not by reading filenames or docstrings.

**Do not repeat that.** Before writing any new file or "finishing" anything described as missing below:

1. `grep`/search the actual codebase for related functionality first. If something looks like it should exist, assume a real implementation already does and go find it before writing a new one.
2. Read the actual SQL migrations in `supabase/migrations/` in order — don't infer schema from `database.types.ts` alone, and don't infer behavior from a `.md` doc without checking the code it claims to describe.
3. After every non-trivial change: `npx tsc --noEmit` and `npx next lint`. Both must be clean before you consider a task done. This project has an untyped Supabase client in at least one historical file (already fixed) that let a broken query pass `tsc` silently — prefer the typed `createAdminClient()`/`createClient()` helpers in `src/lib/supabase/` everywhere, never a raw untyped `createClient(url, key)`.
4. If you find dead code, a naming collision, or a doc that contradicts the code — the code is the source of truth. Fix or delete the doc/dead code; don't reconcile by trusting the doc.
5. Do not fabricate a "% complete" figure anywhere (including this file's own successor) without having actually run something to verify it. The last time that happened, it produced a confident, wrong status report.

---

## 1. What this is

A real-money 1v1 and tournament skill-game platform. Players stake cash on matches of a Connect-N-style game (5×5 board, connect-4, hidden piece inventory with shield/bomb/swap specials, 5-second clock — plus 7 other board variants in `src/lib/game/rulesets.ts`). Two modes only:

- **Ranked** — join a queue at a fixed stake ($1 / $5 / $20), get paired, play, settle immediately. Currently 10% house fee, tiered down to 9% (established) / 5% (elite) for proven players, plus a flat 25¢ fee on the $1 minimum-stake tier. (Raised from an earlier 1%, then 3%/2.75%/1.5%, in a 2026-07-28 repricing — 3% no longer covered real operating cost after processing fees and tax; see `fees.ts`'s inline history and `brand/BRAND.md`.) Ranked also now offers Blitz and Gambit rulesets alongside Classic (`RANKED_RULESET_IDS`), not Classic-only.
- **Tournaments** — pay one entry fee, get seeded into a bracket (7 formats: single-elimination, swiss, bounty, survivor, ladder, satellite, arena), play out the bracket, prize pool (built from entry fees, house keeps 10% standard / 0% "Dollar" format / a *negative* 1% subsidy on "Milestone" events) pays out at the end. Any of the 8 rulesets.

No free-to-play mode. No third mode. The only free thing is a tutorial (not yet built — see §4).

Read `brand/BRAND.md` in full before writing any user-facing copy, UI, or marketing text. It's opinionated and specific (banned vocabulary list, voice rules, color tokens reserved for money-only surfaces, what this brand will never do) and it is binding, not a suggestion.

---

## 2. Architecture map

```
src/lib/game/            Pure domain logic. No I/O. This is the tested core —
                          extend it here first, call it from the impure layers below.
  engine.ts               Board rules: moves, win detection, redaction for the opponent's view.
  rulesets.ts             8 board variants as data (boardSize, connectTarget, timeouts, inventory).
  protocol.ts             The WS wire schema (zod). Client can never assert state/outcome/timing —
                          it names an action, the server decides what happened.
  fees.ts                 Rake tiers, stake brackets, Elo, prize-pool math, the ranked headline rate.
  formats.ts              Tournament formats, payout curves, satellites, ladders, milestones.
  bracket.ts              Pure pairing/standings/advancement functions (knockout, swiss, survivor).
  scheduling.ts           Demand-driven field sizing, sit-and-go, milestone event planning.
  reputation.ts           Skill Index, fee tiers, bot-detection signal shape.
  use-match-socket.ts     Client-side WS hook (ranked + tournament).

src/server/               The authoritative WebSocket match server — a STANDALONE Node process,
                          not a Next.js route (`npm run ws-server`, deployed separately to Fly.io,
                          because Vercel serverless functions can't hold a long-lived socket).
  match-server.ts          Connection auth (signed single-use tickets), rate limiting (token bucket),
                          matchmaking, live game state (in-memory), settlement dispatch.
  sql-match-store.ts       The only thing that touches the database from the match server. Binds
                          match-server.ts to SQL functions. Also owns automatic tournament bracket
                          advancement (see §3).
  entrypoint.ts            The runnable process entry.

src/app/api/ws-ticket/     Issues short-lived signed tickets so the browser never puts a session JWT
                          in a WS query string.

supabase/migrations/      33 migrations, applied in filename order. Read 000006_security_hardening.sql
                          and 000010_settlement.sql / 000012_settlement_ordering.sql closely — they
                          document real bugs found by *executing* the schema against a live database,
                          not just applying it cleanly. That distinction matters: a migration that
                          applies without error proves nothing about whether the functions inside it
                          actually work. Several already have self-test functions
                          (assert_settlement_works(), assert_tournament_completion_works()) that run a
                          real settlement inside a rolled-back transaction — run these against your
                          Supabase project once it's live, and write more of them for anything you add
                          that moves money.

src/actions/              Next.js server actions: auth, deposit, withdrawal, KYC, phone, tournaments,
                          admin-tournaments.
src/components/game/      Real, wired UI: board.tsx, match-result.tsx, piece-tray.tsx,
                          enter-tournament-button.tsx.
brand/BRAND.md            Read before writing any copy. Not optional.
```

### Conventions already established — follow them, don't invent new patterns

- **One tested implementation, DB validates.** Money math (fees, Elo, payout curves) is computed once in TypeScript (`fees.ts`, `formats.ts`, `bracket.ts`) and passed into a Postgres function that re-checks conservation (`fee + payout = pot`, etc.) and raises rather than silently accepting a mismatch. Don't recompute money math inside SQL, and don't trust a TS calculation without a corresponding DB-side check when real money moves.
- **Idempotency by construction, not by convention.** Every money-moving RPC (`reserve_stake`, `refund_stake`, `settle_ranked_match`, `complete_tournament`, `record_tournament_match_result`) is safe to call twice — check the existing pattern (row-lock + terminal-state check, or "does the settlement row already exist") before adding a new money-moving path.
- **`security definer` + `revoke execute ... from anon, authenticated`** on every function a player must never call directly. Only the service-role client (`src/lib/supabase/admin.ts`) calls these. If you add a new money-touching function, follow this exactly.
- **Server-authoritative, always.** The client never sends timing, board state, or a result — it names an intent (`match:move`, `match:resign`) and the server decides. Don't add a client-supplied field that could be trusted for anything money- or fairness-relevant.
- **Two plain queries, joined in JS — not a nested Supabase select.** `database.types.ts` deliberately leaves FK relationships unasserted. Every place in this codebase that needs data across two tables does two `.from().select()` calls and joins in a `Map`, rather than relying on PostgREST's automatic relationship inference. Keep doing this.
- **Threat-model comments.** Nearly every non-obvious security decision in this codebase has an inline `/** THREAT: ... FIX: ... */` comment explaining what attack the code defends against. Keep writing these — they're what let you (and the next agent) tell a deliberate decision from an oversight.

---

## 3. What's actually done (verified as of this handoff)

This handoff pass ran the full §4/§5 list top to bottom, applied every migration to a real, live Supabase project (`grid-clash`, `ajuxrpxpayyaxsrijuir`) one at a time, and executed the codebase's own self-test functions against real seeded accounts after each change — not just "applies cleanly." Nine such probes exist now (`assert_ledger_vocabulary`, `assert_function_dependencies`, `assert_settlement_works`, `assert_tournament_entry_works`, `assert_tournament_completion_works`, `assert_satellite_completion_works`, `assert_bounty_claim_works`, `assert_loyalty_points_mint_works`, `assert_loyalty_redemption_works`) and all nine currently pass. The project's test/seed data was wiped clean afterward — it is an empty, ready-to-use Supabase project again, not a project full of scratch rows.

**Real bugs found by executing (not reading) the schema, fixed in new migrations (022–035):**
- `hash_phone()` called `digest()` unqualified; Supabase installs `pgcrypto` into the `extensions` schema, not `public`, so every self-exclusion identifier check inside `assert_can_wager()` raised — meaning **no phone-verified account could wager at all** until this was fixed. Found only by calling the function for real.
- An orphaned trigger from migration 2 (`update_match_stats`) was never dropped when `settle_ranked_match()` was introduced later and took over the same job — every ranked match **double-counted** `matches_played`/`matches_won`/`lifetime_winnings_cents` (confirmed 2x on a live probe) and wrote nonsense `elo_ratings_history` rows. Dropped; `settle_ranked_match` now writes correct history rows itself.
- `assert_can_wager()`'s weekly loss limit check was silently dropped when migration 21 rewrote the function to add identifier-based exclusion — a player-set `weekly_loss_limit_cents` enforced nothing since then. Restored.
- `milestone_progress` (the SQL view) and `fees.ts` both carried a dead, wrong $1,000 milestone threshold that had drifted from `scheduling.ts`'s real, fully-worked-out $20,000 figure. Both fixed to the one real number.
- `enter_tournament()` broke a **fourth** distinct way during this pass (`CREATE OR REPLACE` with an added parameter creates a second overload rather than replacing in place, making every existing 2-arg call ambiguous) — caught immediately by the self-test suite, fixed same session. If you ever touch this function's signature again, `DROP FUNCTION` first, then `CREATE`, and re-run every `assert_*` that calls it.
- The landing page, root layout metadata, and signup page all still said "2% fee / 51% break-even" in hardcoded copy after `fees.ts` was fixed to the real 1% / 50.51% — found by actually rendering the pages in a browser, not by reading source. All three now derive the number from `FEE_TIERS` so they can't drift again.
- `supabase/config.toml` targeted Postgres 15 locally while the real linked project runs 17, and used the deprecated `[inbucket]` section name — found by running the Supabase CLI for real. Both fixed.
- `scripts/setup-external.sh` generated an `ADMIN_SECRET` env var nothing in the app ever reads (the real admin gate is `users.is_admin`, granted by one manual SQL `UPDATE` — now documented at SETUP.md §1.6, which migration 21 already referenced but never existed until now) — removed. Its Supabase/Stripe/Twilio "connection tests" used bare `curl -s`, which reports success on an HTTP 401 (curl only fails on transport errors without `-f`) — so a wrong API key would have printed a green checkmark. Fixed to `curl -sf` plus the missing `apikey` header Supabase's REST API requires alongside `Authorization`.
- `src/actions/tournaments.ts` (my own new code, this pass) exported a plain `const` from a `"use server"` file — a class of bug invisible to `tsc`/`eslint` that only fails Next.js's actual build transform. Caught by running a real `npm run build`, not just `tsc`. Moved the constant to `fees.ts`; every `"use server"` file in the repo was swept for the same pattern afterward.

**§4 — all eight items now closed:**
1. **Bounty/ladder/arena.** Bounty is a knockout bracket with a side payment on elimination — added to `bracket.ts`/`scheduling.ts`'s bracket-shape checks alongside `single_elimination`/`satellite`, added to `AUTO_ADVANCE_FORMATS`, and — the actual missing piece — **bounty claiming itself was entirely unimplemented** (`tournament_bounties` rows were posted at entry but nothing ever set `claimed_by_user_id` or paid anyone). Fixed in `record_tournament_match_result()`. Ladder and arena are a **deliberate, final** admin-manual (really: not-yet-a-game-mode) decision — ladder is a solo climb with no opponent bracket at all, arena is continuous winner-stays-on seating with no discrete rounds; both need an actual new game loop built (start-run/play-rung/bank endpoints for ladder; seating/rotation logic for arena), which is a materially bigger scope than "wire up auto-advance" and was not attempted. See the comment on `SqlMatchStore.AUTO_ADVANCE_FORMATS`.
2. **Satellite payout curve.** Confirmed broken exactly as suspected (descending curve applied to satellites) — worse, satellites **could not even be created** through the admin UI (no way to set a target). Both fixed: `complete_satellite_tournament()` awards seats at face value plus bubble cash on the remainder, `createTournamentAction` now has a target-tournament picker, `payoutTournament()`/`completeTournamentAction()` both branch on format.
3. **HTTP rate limiting.** Postgres-backed fixed-window counter (`check_rate_limit()`), not Redis — the call is documented in `src/lib/middleware/rate-limit.ts`. Wired into signup, login, deposit, withdrawal.
4. **Admin `fraud_flags` review UI.** Built at `/admin/fraud`. The writers (three DB triggers) were already real and live — confirmed, not assumed.
5. **Dispute resolution.** `match_disputes` table + `file_match_dispute()`/`resolve_match_dispute()`/`list_open_disputes()`, a player-facing "Dispute this result" affordance on `match-result.tsx`, and an admin queue at `/admin/disputes`.
6. **Verification results:** leaderboard UI — the SQL view existed but nothing populated `player_standing`, so it would have shown every player at Skill Index 0 forever; **fee tiers (`FEE_TIERS.established`/`elite`) were therefore silently inert too**, since settlement reads `player_standing.fee_tier`. Built `/api/cron/recompute-standing` (Vercel Cron, every 30 min) to actually compute it, and `/leaderboard` to show it. Referral system — confirmed **missing entirely** (no table, no code); left unbuilt, since building one means inventing reward mechanics never specified anywhere in this brief, not completing something half-built. Background jobs — **two**, not one: the standing-recompute cron plus `/api/cron/detect-automation` (hourly, bot-suspicion scoring, writes `automation_reviews`, which `recompute-standing` itself consumes for skill index/trust score — a real dependency, not an optional extra). Age-gate — was collected at signup but never enforced at the money boundary; now required by `assert_can_wager`/`check_deposit_allowed`. Admin monitoring page — found genuinely broken (its TypeScript interface didn't match what the API route returns; `res.json(): any` hid it from `tsc`) and fixed.
7. **Deployment.** Walked `scripts/setup.sh`/`setup-external.sh` for real against the live project and the actual Supabase CLI (see bugs above). `SETUP.md` §1.6 (grant admin) and the Vercel Cron section were missing entirely and are now written. Did **not** attempt Stripe/Twilio account creation — those need a human with a business identity and are out of scope for an agent regardless of tooling.
8. **Tutorial mode.** Built at `/tutorial` — reuses the real `Board`/`PieceTray` components and `engine.ts` functions against a local scripted bot opponent (no WS connection, no Supabase calls), ends with a clearly-marked mock tournament preview card, links out to real play.

**§5 — all seven funnel items built:**
- §5.1 stake ceiling: `RANKED_STAKE_TIERS_CENTS` cut to `[$5, $20]` (was `[$5, $20, $100]`, which sat exactly on the tournament "gold" tier threshold rather than below it) — a pricing decision, made conservatively per this handoff's own instruction, documented inline in `fees.ts`. Note: `BRACKETS`/`elite.maxStakeCents` ($10,000), which the *original* version of this brief's §5.1 was written against, turned out to be dead code never wired into matchmaking — the real ceiling was the much lower `RANKED_STAKE_TIERS_CENTS` the whole time.
- §5.2 loyalty points: `loyalty_points`/`loyalty_points_entries` + `move_loyalty_points()`, minted at 20% of ranked rake split between both players via a trigger on `platform_ledger`, redeemable as a tournament-entry discount through an extended `enter_tournament(..., p_redeem_points)`. Balance and a redeem checkbox shown next to every `EnterTournamentButton`.
- §5.3 satellites: payout fixed (see §4.2 above), and `/dashboard/tournaments` now shows a loud "Satellite → seat in [Target Name] ($X pool)" banner on satellite cards. Did not build satellite-chaining (a satellite into a satellite) — `planSatellite()` supports it mechanically, but no UI or admin flow for constructing a chain was requested or built.
- §5.4 milestone widget: **reversed as of the 2026-07-28 repricing pass, not built.** A live progress bar was built against the (now-correct, $20,000-threshold) `milestone_progress` view as originally described here, then deliberately removed per `brand/BRAND.md` §9/§11's binding rule ("never surface Milestone progress publicly... the event should land as a surprise, not a publicly-trackable meter"). `src/app/dashboard/page.tsx` carries an inline comment recording the reversal. The threshold fix itself (view now uses the real $20,000 figure) is still live and correct — only the player-visible meter was pulled. **Do not re-add a dashboard milestone meter** — that would directly violate BRAND.md.
- §5.5 title-tier hint: dashboard card reading "Your Skill Index already puts you in range for [Tier]-tier tournaments" once real Skill Index data exists (see §4.6). Reuses `BRACKETS`' skill thresholds purely as a display heuristic — informational only, never a gate.
- §5.6 first-tournament bonus: 100 points, once per rolling week, granted in `enterTournamentAction` after a successful entry, idempotent on `(user_id, week_start)`.
- §5.7 countdown/urgency: admin can now set an optional `starts_at` when creating a contest; tournament cards show a live countdown (`Countdown` component) and a "N seats left" badge when a field is close to full.

`npx tsc --noEmit` → 0 errors. `npx next lint` → 0 warnings. `npm run build` fails in this specific local environment only, for a reason unrelated to this codebase — see §4.7's deployment notes / the final handoff report for detail; it is very unlikely to reproduce on Vercel's own build infrastructure.

**Follow-up pass — live local debugging with the project owner, after the above was handed off:**
- `checkRateLimit()` called `createAdminClient()` outside its try/catch — if `SUPABASE_SERVICE_ROLE_KEY` is ever missing or wrong, the exception is thrown synchronously by the `@supabase/supabase-js` constructor, before the code that was designed to fail open ever runs. This took down **login, signup, deposit, and withdrawal** with a raw 500 the first time it was hit with an incomplete `.env.local`, not just the rate-limit check. Fixed: the whole function body is wrapped now, verified live (a bad key logs a warning and the request proceeds).
- **Operational lesson, not a code bug:** Next.js dev only reads `.env.local` at process boot — editing the file does not hot-reload it. A crash that looks like a code regression after an env change is very often just a stale server process; restart before debugging further.
- The person's own real account (`paulogamet95@gmail.com`) had already signed up against the live Supabase project this pass built and tested against, before the end-of-handoff test-data wipe — the wipe took their `public.users` profile row with it (their `auth.users`/session row was untouched, so login "worked" but the app couldn't find a profile). Restored manually and granted admin. **If you ever wipe test data from a Supabase project a real person might have touched, check `auth.users` for real emails first** — a blanket `TRUNCATE` on `public` doesn't know the difference between a probe account and a real one whose only surviving trace is in `auth`.
- Manually inserting a row into `auth.users` via raw SQL (done here to create a demo login) is not sufficient on its own — GoTrue's Go driver cannot scan a `NULL` `confirmation_token`/`recovery_token`/etc. (needs `''`, not `NULL`), and login additionally requires a matching `auth.identities` row for the `email` provider, which nothing creates automatically outside the real signup flow. Both are needed for a SQL-created account to actually authenticate; a real signup doesn't have this problem.
- `src/lib/stripe.ts` constructs the Stripe client at module load with no guard — visiting `/dashboard/wallet` with no `STRIPE_SECRET_KEY` set is a hard 500, not a degraded page. Not fixed (a real deployment always has this key set, so it's not a deployment blocker), but worth knowing before you go looking for a "wallet page is broken" bug that's actually just a missing env var.

**Unrelated in-flight work found already sitting uncommitted at the start of this pass, not built or audited by this pass:** a substantial, internally-coherent feature set — spectator mode (`spectate:join`/`spectate:leave`, `/watch`), sudden-death replay for ranked draws, ranked ruleset selection (Blitz/Gambit alongside Classic, `RANKED_RULESET_IDS`), a `/practice` free-play sandbox across every ruleset (distinct from `/tutorial` — practice is free-play with no lesson structure, tutorial is guided and ends in a mock tournament preview; not a duplicate of the §0 "second parallel system" kind), bot/anti-cheat detection (`anti-cheat.ts`, `/admin/automation`, `/api/cron/detect-automation`), and Stripe Elements client-side integration. `npx tsc --noEmit`/`npx next lint` were already clean with all of it in the tree. Left as-is; scope it as its own review if it needs one.

**Phase 1 verification pass (2026-08-01):** Re-verified every §4/§5 claim above against live code, live migrations, and the live Supabase project (not just re-reading this file) before resuming further work. Nine of the ten areas checked out exactly as described. Three real findings, all now fixed:
- `list_open_disputes()` had no status filter — despite its name, it returned every dispute ever filed (open/reviewing/upheld/denied alike) to any admin caller. Fixed in `20260801000001_fix_list_open_disputes_status_filter.sql` (adds `and d.status in ('open', 'reviewing')`), applied live, with a new self-test `assert_list_open_disputes_filters_by_status()` — passing. Invisible until now because `match_disputes` has been empty on the live project; would have surfaced the moment any dispute was resolved.
- The live Supabase project had migration `20260726000031_fix_loyalty_redemption_test` applied with no corresponding local file — genuine live/repo drift. Reconstructed and added at `20260726000031_fix_loyalty_redemption_test.sql` (not re-applied live, since it already is; migration 33 fully supersedes its function body anyway, confirmed byte-identical to what's live).
- This file's own fee numbers (1% / 0.6%, "50.51%") were stale — superseded by the 2026-07-28 repricing to 10%/9%/5% (see §1). Corrected throughout. `fees.ts` and `brand/BRAND.md` were already correct and agree with each other; only this document had drifted.
`SETUP.md` was also missing the `detect-automation` cron job and `setup-external.sh` from its documented flow — both added.

**Phase 2 complete (2026-08-01) — tournament economy rebuild:**
- **Three live-breaking bugs found and fixed, all the same root cause.** While self-testing the `commitField` wiring below, tournament/challenge/ladder creation turned out to be completely broken on the live project: `public.rulesets`, `public.contest_eligibility_rules`, and `public.jurisdiction_rules` — all three reference/config tables that migrations 4/6/7 seed at creation — were EMPTY. Near-certainly the same blanket test-data wipe §3 already documents catching a real person's `public.users` row, just never previously checked against config tables that aren't user data. `contest_eligibility_rules` being empty was the most severe: `enforce_entry_eligibility()`'s fail-closed trigger refused literally every tournament entry with "No eligibility rules configured." Restored verbatim in `20260801000003_reseed_rulesets_and_sync_columns.sql` (also synced `rulesets` to two rulesets — gambit, feint — and a `hidden_shields` column added to `src/lib/game/rulesets.ts` since migration 7 was written, which the table had never picked up) and `20260801000004_restore_wiped_reference_tables.sql`, both with passing self-tests. **If you ever run a blanket wipe against this project again, check every table migrations seed with an `insert ... on conflict do nothing`, not just `auth.users`/`public.users`.**
- **`scheduling.ts`'s `commitField` wired** (the other half of the demand-sizing task — `sizeFieldForDemand` was already wired via `admin-tournament-sizing.ts`). New `field_commit_mode` column on `tournaments` (`'fixed'` default, unchanged behavior; `'demand'` treats `field_size` as a ceiling). Reuses the existing `registration_opens_at`/`starts_at` columns as the registration window rather than adding new ones. `commit_tournament_field()` RPC (`20260801000002_demand_sized_field_commit.sql`) atomically rewrites the committed field's money terms and flips it to `full` — self-tested and passing live (seeded two real entrants + five ranked matches between them to actually clear `enforce_entry_eligibility`, not just skip past it). New cron `/api/cron/commit-tournament-fields` (every 5 min, wired into `vercel.json`) finds demand-mode tournaments whose window closed, refunds under-floor contests in full (cancels) or snap-down overflow (latest registrants by `seat_number`, bracket formats only) via the existing `move_balance` idempotent path, then commits. Admin UI: a "Demand-sized" checkbox on `create-tournament-form.tsx`, requires `startsAt`.
- **Rank-tiered tournament fees built** (the hard-stop deliverable, confirmed by the person before implementation). Because a tournament pools its whole field at one rake rate — unlike ranked, where each match is a private pot — this is a per-CONTEST tier, not a per-entrant discount: a tournament is created gated to a minimum `player_standing.fee_tier` (new `tournaments.min_player_tier` column, default `'standard'` — every pre-existing/not-yet-set tournament is unaffected) and charges that tier's rate for the whole field. Rates, `src/lib/game/formats.ts` `FORMAT_TIER_RAKE_BPS`: Knockout/Bounty/Satellite/Arena 14% → 12.5% (established) → 7% (elite); Swiss/Survivor 15% → 13.5% → 7.5%; Ladder 11% → 10% → 5.5% — elite is exactly half of standard for every format, the same ratio `FEE_TIERS.elite` already is to `.standard` in ranked. Gate enforced in `check_contest_eligibility()` (`20260801000005_rank_tiered_tournament_fees.sql`, self-tested and passing: refuses a sub-tier player including one with no `player_standing` row at all, admits them once raised to the required tier). `planTournament()` takes a `tier` param and prices from `FORMAT_TIER_RAKE_BPS` instead of the format's flat rate; `createTournamentAction`/admin UI expose a "Player tier required" selector; tournament cards show a tier-required badge. Satellites remain the intended low-bankroll on-ramp into gated tiers — Elite entry fees were deliberately left real rather than cut toward zero. The floor/refund half of the hard stop's constraint (d) is the `commitField`/`commit_tournament_field` work above, not new work for this bullet.
- `database.types.ts` regenerated from the live schema twice this phase to pick up all of the above.

**Phase 3 complete (2026-08-01) — social tab:**
- **Critical, unrelated live bug found and fixed while building this phase — bigger than Phase 3 itself.** `signUpAction`'s DOB/terms-of-service write and `verifyPhoneCodeAction`'s phone-verified write have both been silently failing (permission denied, never checked) since `20260724000006_security_hardening.sql` revoked insert/update/delete on `public.users` from `authenticated` — a correct fix for its own finding (PII leak) that broke two write paths written afterward without being tested against the actual grants. **Confirmed live: the real account owner's own account (`paulogamet95`) and a second real account (`orcainf`) both had `date_of_birth_self_attested`/`terms_accepted_at` still null and `account_status` stuck at `'phone_pending'`, meaning `assert_can_wager`/`check_deposit_allowed` had been refusing every real signup on this platform outright** — the age gate and phone verification were never actually completing for anyone. Also found: nothing in the codebase ever transitioned `account_status` from `'phone_pending'` to `'active'` at all (grepped for it — the only writer was an unrelated admin fraud-review action); account activation on phone-verify success was simply never implemented. All fixed in `src/actions/auth.ts` (`signUpAction`, new `completeAttestationAction`), `src/actions/phone.ts` (`verifyPhoneCodeAction`, now also flips `account_status` `phone_pending → active`, scoped to never touch `suspended`/`banned`/`kyc_pending`), `src/actions/withdrawal.ts` and `src/actions/deposit.ts` (same silently-broken `stripe_connect_account_id`/`stripe_customer_id` persistence, same fix — service-role client for every `users` write, matching how the rest of this codebase already does it). A `CompleteAttestationPrompt` on the dashboard covers the two already-stuck real accounts going forward; their DOB/terms are deliberately **not backfilled** with a fabricated value — a self-attestation isn't something an agent can supply on a real person's behalf.
- Also found while wiring the same page: `public.users_select_own` (RLS) is self-only, and `public.public_players` (the security-hardening fix's own allowlisted view) is the *only* way any code — anon or authenticated — reads another player's row at all. Several new files (`friends.ts`, `auth.ts`'s referral lookup) initially queried `users` directly for another player and would have silently returned nothing; caught before commit and fixed to read `public_players`. Extended that view with `avatar_url` (`20260801000007_public_players_avatar.sql`) rather than adding a second, narrower view.
- **Profile** at `/players/[username]`: username, avatar (Storage bucket `avatars`, public read, owner-only write — folder-per-user-id RLS), equipped title (reuses `player_titles`/`TITLE_TIER_MARK`, extracted to `src/lib/game/titles.ts` so `/leaderboard` and this page read one copy instead of risking the exact drift this codebase's own history is full of), Skill Index/trust band. Self-view adds avatar upload and the challenge-preferences toggle.
- **Friends**: new `friendships` table (request/accept/decline), gated at the RLS layer for insert/select/delete and through a `respond_to_friend_request()` RPC for accept/decline specifically (a raw RLS UPDATE policy turned out to be untestable against this project's own admin/owner connection, which bypasses RLS by default — the RPC gives an explicit, testable `raise exception` instead, same shape as everything else money-adjacent in this codebase). `/dashboard/friends`: search, incoming/outgoing requests, friends list. `account_links` (the collusion-detection table) was confirmed to be a completely different, RLS-deny-all thing and was not touched or reused.
- **Referral-linked add**: confirmed no referral system exists anywhere (still true, matches §3/§4's prior finding) — built the minimal, non-financial version the person's own phrasing implied rather than a reward/payout system (out of scope, needs a product decision this brief doesn't make): `users.invited_by_user_id` captured once at signup from a shared username, surfaced on `/dashboard/friends` as a one-click "add as friend" prompt. No points, no cash, no gated mechanics.
- **Invite to a 1v1 wager**, from a friend's profile — hands off into the wager-marketplace phase, does not build a second match-creation path. Wired the existing-but-unreachable `public.challenges`/`challenge_preferences` (found more unwired than `BRACKETS` — no INSERT/UPDATE policy at all, no functions, no callers anywhere) via `create_challenge()`/`respond_to_challenge()` RPCs: friend-gated (only a mutual, accepted `friendships` row can be challenged — the real harassment mitigation, not just the target's own opt-in), respects the target's `accepts_challenges`/stake-range preference, excludes Purist (same solved-board reasoning as tournament creation). Stops deliberately at `status = 'accepted'`, `match_id` still null — turning that into a live match needs `match-server.ts` to learn a third `LiveMatch.kind` alongside `"ranked"`/`"tournament"`, which is the wager-marketplace phase's job.
- `database.types.ts` regenerated again to pick up `friendships`, `create_challenge`/`respond_to_challenge`/`respond_to_friend_request`, and the `users`/`public_players` column additions.

**Off-queue critical fix (2026-08-01) — tournament matches could never complete via real gameplay.** Found while investigating why match data is unavailable for a requested draw-rate/game-length instrumentation report (not part of any numbered phase). `src/server/sql-match-store.ts`'s `recordTournamentResult()` was passing `match-server.ts`'s freshly-generated, never-persisted `match.id` as `p_match_id` into `record_tournament_match_result()` on every real tournament match completion. No `public.matches` row is ever created for a tournament match, so this violated `tournament_matches_match_id_fkey` **every single time** — confirmed empirically against the live project with a rolled-back transaction, not just read from the schema. Net effect: **the WS server's real `settle()` path has never been able to complete a single tournament match**; only the admin manual-completion path (`completeTournamentAction`, which supplies a full ordered finisher list directly to `complete_tournament`/`complete_satellite_tournament` and never calls this function at all) has ever worked. This is why Phase 1's self-tests for satellite/bounty completion passed — they exercise the manual path, not real bracket-by-bracket gameplay. Fixed: pass `undefined` (→ SQL `DEFAULT NULL`) instead — `tournament_matches.match_id` is nullable by design for exactly this reason. New self-test `assert_tournament_match_result_match_id_contract()` (`20260801000008_...sql`) pins the contract so this can't silently regress. **Not attempted**, deliberately: actually creating a `matches`/`match_replays` row per tournament match (the data `loserId`/`reason`/`moveSequence`/`durationSeconds` is already computed and threaded through for, then dropped) — doing so would also make tournament matches count toward `check_contest_eligibility`'s ranked-match-count gate, become filable via `file_match_dispute()` (currently "ranked matches only" by UI convention, not enforcement), and fire the collusion-detection trigger against tournament pairings. All fixable, but a bigger and more sensitive change than fixing an active crash — flagged for a dedicated pass, not bundled into this one.

**Phase 4 complete (2026-08-05) — wager marketplace, mechanic A only (equal-stake).** Human-confirmed hard stop before any of this was built: equal-stake wagers only for launch — handicap/spread and side-bet mechanics are explicitly out of scope, pending separate confirmation; fee tier 12% standard / 11% established / 6% elite, charged at the **winner's** tier (same precedent `settle_ranked_match`/`loadParticipants` already established for ranked); anti-abuse (collusion check + repeat-pairing cap) kept exactly as scoped, not softened. See §5.8 below for the fee-tier reasoning specifically, as requested at confirmation time.
- **Schema/RPCs** (`20260801000009_wager_marketplace.sql`): ledger vocabulary extended (`wager_entry`/`wager_payout`/`wager_refund` reasons, `wager_rake` platform-ledger type — `assert_ledger_vocabulary()` updated in the same change, per the lesson already documented in `20260724000008_ledger_vocabulary.sql`). `challenges` (built unreachable in Phase 3, now wired) reused for both friend challenges and open wagers — `target_id` made nullable, `challenger_reservation_id`/`acceptor_reservation_id`/`started_at` added, `'completed'` added to the status check. New: `reserve_wager_stake`/`refund_wager_stake` (mirror `reserve_stake`/`refund_stake` rather than modify them — those hardcode ranked-specific ledger reasons internally), `assert_wager_pairing_not_rate_limited` (max 5 wagers/week between any pair), `create_open_wager`, `accept_open_wager`, `cancel_wager`, `settle_wager_match` (mirrors `settle_ranked_match`: idempotent by `match_id`, conservation-checked, `matches.ranked = false`, deliberately **no** Elo/`elo_ratings_history` writes), `expire_stale_wagers` (cron sweep, two branches — pending-past-24h and accepted-but-never-started past a 30-minute grace on `started_at`). `create_challenge`/`respond_to_challenge` updated in place: collusion + rate-limit checks added, stake reserved immediately on creation/acceptance instead of never. Also fixed in the same pass: `check_contest_eligibility`'s ranked-match count had no `ranked = true` filter — harmless while `matches` only ever held ranked rows, would have silently counted wager matches toward tournament eligibility the moment `settle_wager_match` started inserting `ranked = false` rows.
- **Two follow-up gaps found and closed before the UI shipped, not after:**
  - `WAGER_MIN_STAKE_CENTS`/`WAGER_MAX_STAKE_CENTS` ($25/$5,000, `fees.ts`) existed as constants but were never enforced anywhere — `create_open_wager`/`create_challenge` only checked `stake > 0`. A bound that lives only in TypeScript isn't a real bound under this codebase's own "server decides, client never asserts" rule. Fixed in `20260801000010_wager_stake_bounds.sql`, self-tested (`assert_wager_stake_bounds_enforced`).
  - The only SELECT policy on `challenges` (`challenges_select_involved`) is `challenger_id = auth.uid() OR target_id = auth.uid()`. An open wager sits with `target_id` null until accepted — under that policy alone, **no one but the poster could ever see an open wager**, meaning the open board could not have worked at all. Added `challenges_select_open_board` (`20260801000011_wager_open_board_rls.sql`, `authenticated` role, `status = 'pending' and target_id is null`), self-tested by asserting the policy's exact predicate (RLS enforcement itself isn't testable from this project's SQL-editor connection — see §3's Phase 3 entry on the same limitation).
- **Self-tests** (`assert_wager_settlement_works`, `assert_wager_anti_abuse_works`, `assert_expire_stale_wagers_works`, plus the two above): all pass, though `assert_wager_settlement_works` currently reports **skipped** on live data (needs two distinct active/phone-verified/$100-funded accounts; the live project has exactly one such account right now — see Phase 3's account-activation finding). Written to run for real the moment a second account clears that bar; not a hollow check. `assert_wager_anti_abuse_works`/`assert_expire_stale_wagers_works` both run and pass for real against live data today.
- **Match-server layer:** `LiveMatch.kind` extended to `"ranked" | "tournament" | "wager"`. Wagers get their own rendezvous pair (`wager:join`/`wagerWaiting` map), structurally identical to the tournament pattern (two known players, not a queue) — including the same defense-in-depth `arePlayersLinked` re-check at match-start, since an accepted wager can sit up to 30 minutes before both sides actually connect. Settlement gets its own branch (`settleWagerMatch`, `wager:over`) rather than reusing `match:over` — a wager's result deliberately has a `payoutCents` but no `eloDelta`, and `match:over`'s `eloDelta` field is non-optional, so reusing it would mean either a fake `0` (reads as "confirmed no change" instead of "not applicable") or loosening a field ranked still depends on. Sudden death (a natural draw) gets `wager:sudden_death`, capped at `MAX_RANKED_SUDDEN_DEATH_ROUNDS` same as ranked (not left uncapped like tournament) — money shouldn't sit in escrow indefinitely waiting for a decisive board, same reasoning as ranked already applies.
- **Fixed while wiring, not new scope:** `MatchResultCard`'s payout display, win-celebration treatment, and dispute affordance were all gated on one combined `hasRankedTerms = payoutCents !== undefined && eloDelta !== undefined` flag — which would have silently hidden the payout and the "Dispute this result" button on every wager win, since a wager result never sets `eloDelta`. Split into independent `hasPayout`/`hasEloDelta` checks. Confirmed `file_match_dispute()` itself has no ranked-only restriction (checked the live function body rather than assuming) — a wager match gets a real `matches` row via `settle_wager_match`, so it's dispute-eligible exactly like ranked.
- **UI:** `/dashboard/wagers` (post to the open board, accept from the board, respond to an incoming friend challenge, cancel your own pending post, "Play" through to an accepted wager), `/play/wager/[challengeId]` (mirrors `/play/tournament/[tournamentMatchId]`'s connect/waiting/matched/over states). `respondChallengeAction`/`createChallengeAction` (built in Phase 3, never wired to a live match since the match-server didn't understand wagers yet) now actually complete the loop — their stale "isn't live yet" comments/copy updated.
- `database.types.ts` regenerated three times this phase (after the core migration, the stake-bounds fix, and — not needed for types but run anyway for consistency — the RLS policy migration).
- `npx tsc --noEmit` → 0 errors. `npx next lint` → 0 warnings. No test runner exists in this project (checked `package.json`, no `*.test.ts`/`*.spec.ts` anywhere) — verification for this phase is tsc/lint plus the executed SQL self-tests above, consistent with how every prior phase in this file was verified.

**Phase 5 complete (2026-08-05) — final pass.** `npx tsc --noEmit`/`npx next lint` re-run clean across the whole project (not just Phase 4's files). Every `assert_*` self-test in the project — 22 total, not just the 5 Phase 4 added — was re-run in one sweep to confirm nothing regressed cumulatively across the whole session, not just within Phase 4. Three real breakages found, all caused by Phase 4's own correct new behavior landing against self-tests written before that behavior existed (none are product bugs — in every case the RPC being tested was correctly refusing an ineligible probe account; the bug was in what the self-test assumed about that account):
- `assert_create_challenge_respects_friendship_and_preferences` (Phase 3) used a 500-cent stake, below Phase 4's new $25 floor, and picked "any two distinct users" rather than two eligible ones — broke a second way once `respond_to_challenge` started reserving a real stake and hit this project's one `phone_pending` probe account. Fixed to use a valid stake and skip honestly (matching every Phase 4 self-test's own convention) when fewer than two eligible accounts exist.
- `assert_tournament_entry_works` and `assert_loyalty_redemption_works` (both pre-Phase-2) assumed their probe account already had 5+ real ranked matches on record — true of no account on this live project (confirmed empirically: the qualifying account has zero matches, ranked or otherwise, so this predates Phase 4's `ranked = true` filter fix entirely and isn't caused by it). Fixed by seeding 5 real `ranked = true` matches for the probe pair and cleaning them up after, the same pattern `assert_commit_tournament_field_works` already used — confirmed the collusion-detection trigger (`flag_suspicious_match`, fires above 10 matches between one pair) never trips at 5. All three fixes in `20260801000012_phase4_selftest_regressions.sql`.

Final self-test tally, this pass: 22 exist, 0 raise, 13 report `ok` against real executed logic, 9 report an honest `skipped: <reason>` (all because this live project currently has exactly one active+phone-verified+funded account — see Phase 3's account-activation finding). None were skipped by writing around a failure; every skip names the specific precondition it's missing.

**Phase 6A (2026-08-05) — extended game-depth investigation. Data only — `engine.ts`/`rulesets.ts` untouched, per the explicit freeze ("core mechanics, win conditions, randomness/variance are paused pending human review, legal-classification implications, out of scope for an agent").** 4,000 bot-vs-bot games per ruleset (40,000 total), real `engine.ts`/`bot.ts`, no mocking — same methodology as the earlier draw-rate pass, extended with move-number-at-end and a shielded-cell-targeting counter.

| ruleset | P1 win% | P2 win% | draw% | mean end move# |
|---|---|---|---|---|
| classic | 68.5 | 31.4 | 0.0 | 10.5 |
| blitz | 69.6 | 30.4 | 0.0 | 10.5 |
| purist | 39.1 | 29.6 | 31.2 | 19.2 |
| siege | 60.9 | 38.9 | 0.2 | 16.3 |
| demolition | 67.2 | 30.7 | 2.1 | 10.7 |
| fortress | 38.1 | 34.5 | 27.5 | 19.4 |
| shuffle | 67.6 | 32.3 | 0.1 | 10.5 |
| gambit | 68.0 | 31.1 | 1.0 | 10.7 |
| feint | 69.3 | 30.8 | 0.0 | 10.5 |
| sprawl | 60.5 | 39.5 | 0.0 | 16.2 |

**Finding 1 — a real, structural first-move advantage, not the population-level fairness question.** Who *gets* slot 1 is already fair and unbiased across a season (`coinFlipGivesFirstSlotTo`, seeded per-match, verified elsewhere in this codebase). This is a different question: once assigned, slot 1 wins 60–70% of the time in every bomb/normal-heavy ruleset (classic, blitz, demolition, shuffle, gambit, feint, siege, sprawl) — meaning a single match's outcome leans heavily on a coin flip neither player controls, which is worth real scrutiny for a product whose entire legal/brand positioning is "genuinely skill-based." The two shield-heavy, bomb-light formats (fortress: 4 shields/1 bomb; purist: 0 specials at all) are both far closer to even (38–39% vs 30–35%) and far more likely to draw (27–31% vs ~0–2%) — a real, visible tradeoff between decisiveness and fairness that tracks inventory shape, not chance.

**Caveat, stated plainly:** this measures the edge as expressed through this project's own practice bot (`bot.ts`, deliberately simple, not solved — see its own header). A stronger or weaker AI could show a different magnitude. Treat 60–70% as a directional, real signal that slot 1 matters a lot in this design, not a precise measurement of the human-vs-human edge.

**Finding 2 — the "bluff-layer proxy" came back a clean, expected null result, and here's exactly why.** `feint` (the one `hiddenShields` ruleset) and `classic` (its explicitly-isolated baseline — same board/inventory/clock, only the shield-visibility rule differs) are statistically indistinguishable on every metric above, and the shielded-cell-targeting rate is **0.00% in every ruleset, including feint.** This is not "bluffing doesn't matter" — it's that bot-vs-bot play cannot measure it at all: `chooseBotMove` receives the true, unredacted `GameState` (see `bot.ts`'s own header — "there's nothing hidden about the board itself" was true when written, before `hiddenShields` existed to make the board itself sometimes hidden), so the bot always knows exactly which cells are really shielded and routes around them by construction. **A real measurement of FEINT's bluff value needs either logged human games (none exist yet — same "no data" state the original draw-rate instrumentation found) or a deliberately blinded bot variant that reasons over `RedactedGameState` the way a human opponent actually has to — a nontrivial build, not attempted here as it's well outside "investigate and report."**

**Design menu — proposals only, not applied, human decides:**
1. **Accept the first-move edge as a known, communicated structural property** (heads-up poker's button/dealer edge is the closest real-world analog — accepted there as part of the game, not hidden). Lowest-effort option; requires no mechanic change, only being honest about it if it's ever asked about.
2. **Compensate slot 2** with a small resource edge (e.g., one extra normal piece) — the poker "big blind gets to act last preflop" shape. Would need its own simulation pass to check it doesn't overcorrect, and directly touches `rulesets.ts` inventories, so it's exactly the kind of change this session was told to leave alone.
3. **Re-examine board size / connect target** for the bomb/normal-heavy rulesets specifically, since fortress/purist's very different profile suggests the edge tracks how much of the game is "claim territory fast" versus "defend what you claimed" — a structural, not cosmetic, question.
4. **Build a redacted-state-aware bot** before making any FEINT-specific call — right now there is no real signal on whether hidden shields are worth the added rule at all, only confirmation that the current measurement method can't see it.
5. **Do nothing this cycle** — ship wagers/retention work (Phase 4/6B) on the current mechanics, revisit with real human match data once ranked/wager volume exists, same "get the number first" standard already applied twice this session.

None of these five are implemented. `engine.ts`/`rulesets.ts` have zero diffs from this phase.

**Phase 6A freeze override (2026-08-05) — Feature A, Strategic Score win condition.** Phase 6A above froze "core mechanics, win conditions, randomness/variance... pending human review, legal-classification implications, out of scope for an agent." The project owner explicitly, knowingly lifted that freeze the same day, in writing, specifically to authorize building and live-wiring a Strategic Score win condition: **"I am the project owner, and I am knowingly and explicitly lifting that freeze now... the legal-classification question it was protecting against has not been independently resolved — it's being accepted as a known, owner-accepted risk, not a resolved one."** Recorded here verbatim per that instruction, not softened. This override covers wiring the mechanism only — it does not resolve §4.3's separate, still-open legal-review item, and does not authorize activating Strategic Score for real users (see below).

**Feature A, built this pass, all three phases:**
1. **`src/lib/game/scoring.ts`** — pure, deterministic Strategic Score engine. Replays an already-validated move list and produces a fully attributable ledger across seven components (four-in-a-row, board control, threat density, dual threat, positional dominance, forced-response pressure, strategic pressure, threat neutralized), each a boolean trigger over board state — no lookahead, no opponent modeling, no randomness. Does not touch `engine.ts`: a match still ends exactly when `engine.ts` already ends it; this only decides who is credited as the winner at that point. Self-tested (`scripts/test-scoring-engine.ts`, `npm run test:scoring`) — determinism, isolated per-component correctness, the brief's own worked-example point values, ledger attributability, illegal-replay rejection, and a wire-shape contract test against exactly how match-server.ts builds replay entries.
2. **`scripts/simulate-strategic-score.ts`** — real `engine.ts`/`bot.ts` bot-vs-bot simulation, no mocking, same methodology as this file's own earlier draw-rate pass. Balanced self-play, 4,000 games/ruleset, all 10 rulesets: reversal rate (strategic winner ≠ traditional winner) is under 2% for 8 of 10 rulesets, but purist/fortress — the two heavy-draw, low/no-special rulesets — see ~27–30% reversal and resolve ~27–30% of traditional draws into a decisive strategic winner. A separate, deliberately blind "rusher" policy (never blocks, only ever extends its own line) vs. balanced `bot.ts`, 10,000 games on Classic, **found a real exploit on the first run**: the rusher's win rate went from 10.9% (traditional) to 21.6% (strategic) — every original component rewarded offense, none rewarded defense, so blind aggression out-scored real defensive play. Fixed by adding `threat_neutralized` (symmetric to the existing `forced_response`, from the defender's side), not by nudging existing constants. Re-run at n=10,000: 0.5pp effect against a 0.6pp margin of error — no longer distinguishable from noise. Both scripts are real, runnable, honest-output tools, not one-off scratch work — kept in `scripts/` for whoever picks §4's remaining Strategic Score items back up.
3. **`src/server/match-server.ts`** — `LiveMatch.strategicReplay` accumulates a structured replay alongside the existing `moveSequence` display log (reset per sudden-death round — Strategic Score only ever scores the round that actually decided the match). In `settle()`, after the sudden-death branch and before `match.settled` flips true, a ranked match on a flagged-in ruleset has its `winnerSlot` recomputed from `computeStrategicScore` instead of trusted from engine status; everything downstream (fee tier, Elo, payout) already derives from `winnerSlot`/`isDraw`, so no other settlement code changed. Wrapped in try/catch, fails open to the traditional winner on any error — a scoring bug must never block or corrupt a real-money settlement. Scoped to `kind === "ranked"` only (tournament brackets never reach this branch in a draw state; wager wasn't part of the Phase 2 simulation) and excludes `forcedWinner` endings (resign/abandon have no "who played better" question).

**Not done, deliberately — activation.** `STRATEGIC_SCORE_ENABLED_RULESETS` (`scoring.ts`) is empty. The code path above is a live no-op for every ruleset today. Populating it for a given ruleset — turning this on for real matches — is a separate, deliberate decision this pass did not make: it needs (a) §4.3's legal-classification review, still open and not touched by the owner's override above, and (b) a per-ruleset simulation pass for anything beyond Classic before that ruleset is added (Phase 2 only adversarially tested Classic; the other 9 rulesets have balanced-self-play data but no rusher-style exploit test). Also not done: UI copy for a match where the traditional line-completer loses on Strategic Score (`match:over`'s `reason` field can now read e.g. `"line"` while `result` reads `"lost"` for the player who completed it) — no real user can hit this yet since the flag is off, so this was left as a named follow-up rather than a guess at copy nobody has reviewed.

## 4. What's still genuinely open

Everything from the original §4/§5 lists is closed (see §3). What remains is what no amount of code can close:

1. **Ladder and arena are schema-only.** Building their actual gameplay loops (a solo bank-or-continue climb for ladder; continuous winner-stays-on seating for arena) is a new-game-mode-sized effort, not a bug fix. Scope it as its own project if it's wanted.
2. **Referral system.** Confirmed not to exist anywhere. Needs actual product decisions (reward shape, fraud considerations, payout rules) before it's buildable — none of that is specified in this brief.
3. **Legal review (§6).** Nothing here is a substitute for it. `jurisdiction_rules`'s seed data is explicitly a starting point for counsel, not legal advice.
4. **Real account creation** (Supabase production project already exists and is verified working; Stripe, Twilio) needs a human. See the final handoff report for exact next steps.
5. **Wager mechanics B/C (handicap/spread, side-bets)** — deliberately not built per the Phase 4 hard stop. Mechanic A (equal-stake) is the only wager type this codebase supports; B/C need their own scoping and confirmation, not an extension of what exists.
6. **No per-user exposure cap across concurrent wagers.** The $25–$5,000 per-wager bound and the 5-per-week same-pair rate limit (§3, Phase 4) are the only caps that exist — nothing limits how many *different* wagers one account can have open/accepted at once, or a rolling-window dollar total across all of them (`assert_can_wager`'s existing weekly loss limit is the closest thing, and it's opt-in/player-set, not a platform floor). Worth a deliberate decision before wagers see real volume, not an oversight to silently patch.
7. **Wager eligibility confirmed inherited, not newly gapped:** `reserve_wager_stake` calls the same `assert_can_wager()` ranked already uses, so self-exclusion, jurisdiction, and (if a player has set one) their own loss limit all already gate wagers with no new code. Stated explicitly here because it's exactly the kind of "assumed inherited, actually silently missing" bug this file's own history (Phase 3's account-activation finding, the tournament-match-FK bug) has repeatedly turned up elsewhere — this one was checked, not assumed.
8. **Strategic Score activation (Feature A, §3).** Built, simulated, live-wired, self-tested — but `STRATEGIC_SCORE_ENABLED_RULESETS` is empty and stays that way until a human decides otherwise. Needs: §6's legal review specifically applied to a win condition that changes who a real-money match pays (the Phase 6A override above covers building it, not activating it); a rusher-style adversarial simulation pass for any ruleset beyond Classic before it's added to the enabled set; and the reversed-outcome UI copy (a player who completed the line but lost on Strategic Score) written and reviewed before real players can see it.
5. **Everything in this file should be re-verified** the first time real user traffic hits it — every self-test in §3 was run against small, synthetic probe data, which proves the logic is correct but not that it holds up at the platform's real ~5,000-user scale.

---

## 5. Ranked-vs-Tournament funnel strategy — build this, don't just note it

**The problem stated by the person who owns this, as originally framed:** ranked is cheap (1%) and always available. Left alone, that's a reason for a good player to *never* look at tournaments, which are both more profitable for the house (10% vs 1%) and the more marketable, exciting, "event" surface of the product. Ranked should exist and stay cheap — it's the low-friction front door and the skill-signal engine (Elo) — but the product needs real, structural pulls toward tournaments, not just hoping people click the other tab.

**STALE AS OF THE 2026-07-28 REPRICING — READ BEFORE ACTING ON THE PARAGRAPH ABOVE.** Ranked is no longer 1%; it's now 10% standard / 9% established / 5% elite (fees.ts, brand/BRAND.md, both current — see §1). The "10% vs 1%" gap this whole section's reasoning leans on doesn't exist anymore: a brand-new ranked account and a brand-new-account tournament entry both start at the same 10% headline rate. This *sharpens* the funnel problem rather than solving it — ranked is no longer structurally cheaper, so the pull toward tournaments now has to come entirely from the mechanisms below (loyalty points, satellites, titles, urgency, stake ceilings) rather than partly from a rake gap that's gone. Phase 2 of the active work queue (see CLAUDE_CODE_BRIEF.md's own commit history) is building a rank-tiered tournament fee schedule specifically to re-establish a coherent, if different, gap: tournament payouts staying realistically above ranked's at equivalent spend even though the entry-level rate now matches.

This is not a new problem — it's the exact shape of "cash games vs. tournaments" that the entire online poker industry has spent 20 years solving, plus what mobile real-money tournament platforms (Skillz being the largest, ~$300M+ paid out historically, entirely tournament-framed even for 1v1 matches) do differently. Below is what's actually proven to work, adapted to this codebase, not generic advice. Build these — most of them are additive (new tables/functions alongside what exists), not rewrites.

### 5.1 Rank-gated stake ceilings that plateau below tournament entry (the person's own instinct, confirmed correct)

This is exactly how poker cash games and tournaments already relate: cash game rake is capped in absolute terms even at high stakes (a $5/$10 game might cap rake at $10 regardless of pot size), while tournament buy-ins have no such ceiling — a serious player who wants to put more than the cash cap in play *structurally has to* go to a tournament to do it.

- `fees.ts` already has `BRACKETS` (bronze/silver/gold/elite, gating stake *ranges* by skill index + verification) topping out at `elite.maxStakeCents = 1,000,000` ($10,000) and `RANKED_STAKE_TIERS_CENTS = [500, 2000, 10000]` as the actual selectable amounts.
- Audit this against the tournament side: `complete_tournament`'s title-tier thresholds start "obsidian" at `entry_fee_cents >= 25000` ($250). Right now a ranked player can stake $10,000 in ranked — an order of magnitude above where a tournament entry starts feeling exclusive. That's backwards for the funnel goal.
- **Fix:** lower ranked's real ceiling (not necessarily `elite.maxStakeCents` — pick the actual number with the person, but structurally it should sit *below* where tournament entries start feeling like "the real stakes"), and make sure tournament entry fees have meaningfully higher ceilings than anything ranked offers. The gap between "the most you can ever stake in ranked" and "the least a serious tournament costs" should be small or negative — i.e., a player who outgrows ranked's ceiling has nowhere to go *but* a tournament.

### 5.2 A loyalty-points economy funded by ranked's own rake, spendable only on tournament entries

This is the single most proven retention mechanic in the entire real-money gaming industry (poker "rakeback"/VIP points — every major operator runs some version of it) and it maps almost exactly onto what's needed here: convert ranked activity into a currency that's worthless anywhere except the thing you want people to try.

- New table, e.g. `loyalty_points` (`user_id`, `balance`, ledger table for audit — same pattern as `balance_entries`).
- Every `ranked_rake` entry written to `platform_ledger` (already happens, at settlement) also mints points — e.g. 1 point per $1 of rake paid (tune the number; the mechanism matters more than the exact rate). This is *additive* to `settle_ranked_match` — a trigger on `platform_ledger` insert, or an explicit call alongside it, following the same idempotent, security-definer pattern as everything else here.
- Points are redeemable **only** as a discount or full comp on a tournament entry fee, never convertible to cash, never usable in ranked. Build the redemption function the same way `move_balance` works (idempotency key, atomic, audited).
- Surface the balance right next to every tournament's "Enter" button: "You have 340 points — $3.40 off this entry" (per `BRAND.md`'s own voice rule: numbers speak, don't editorialize).
- Originally framed as not touching a ranked-1%/tournament-10% differential — that differential no longer exists (see the stale-numbers note above §5.1: ranked is now 10% standard too). The mechanism itself is unaffected by the repricing — it still gives the house a controlled, self-funded way to make tournament entry cheaper *for people who already generate ranked revenue*, a straight transfer of the thing they're already paying for into the direction the business wants them to go — but it's now carrying more of the funnel's weight than originally intended, since the rake gap it was meant to sit alongside is gone.

### 5.3 Satellites — already built, not yet surfaced. This is the highest-leverage item on this whole list.

The `format_id = 'satellite'` / `satellite_target_tournament_id` mechanic already in this schema *is* the single most famous and effective funnel mechanic in the entire industry — it's literally how poker turns cash-game grinders into main-event players (the Chris Moneymaker story: a $39 satellite entry turned into a World Series Main Event seat and a $2.5M win, and single-handedly created the 2000s poker boom). The mechanism is simple and already representable in this schema: a cheap entry fee, winner(s) get a *ticket* into a bigger, more expensive tournament instead of cash.

- Confirm (per §4.2) satellite payouts are flat/equal-value tickets, not a descending curve — this is structurally what makes a satellite a satellite rather than just a cheap tournament.
- Build the UI to make this loud: a satellite's card should say what it's a satellite *into*, by name, with the target tournament's prize pool front and center ("$5 entry → win a seat in the $25,000 Obsidian Invitational"). This is a much stronger hook than a generic "enter this tournament" card.
- Consider a multi-step satellite ladder (cheap satellite → seat in a bigger satellite → seat in the target event) for the highest-value tournaments — `planSatellite()` already exists in `formats.ts`; check whether it supports chaining and extend it if not.

### 5.4 Milestone tournaments as visible, house-funded hype events

`CONTEST_FEE_BPS.tournament_milestone = -100` means the house *adds* money to these — the direct equivalent of a poker "guarantee" or DFS "overlay," both proven to drive a disproportionate amount of attention and signups relative to their actual frequency, because "the house is subsidizing this one" is a genuinely compelling, honest hook (and it's the one place inflated marketing language is actually justified by real math, consistent with the brand's own "numbers speak" rule).

- `progressToNextMilestone()` already computes how close the platform is to unlocking the next one. Put this on the dashboard as a visible, live-updating progress indicator — "Next Milestone event unlocks at $[X] platform profit, currently at $[Y]" — turning an abstract backend number into something a ranked-only player has a reason to go check on.

### 5.5 Titles are already tournament-exclusive — lean into it, make it visible

`BRAND.md` §9 already establishes that titles (Dollar/Bronze/Silver/Gold/Obsidian) are earned *only* by winning a tournament at that tier — ranked play, however much of it, cannot earn one. This is already the right shape (status-seeking pull toward tournaments) but needs a visible trigger: when a ranked player's Skill Index/Elo crosses into a range that would make them competitive at a given tournament tier, show them that, specifically — "Your Skill Index already qualifies for Obsidian-tier tournaments" is a concrete, personalized invitation, not a generic ad.

### 5.6 A small "first tournament of the period" bonus (the "missions" pattern)

Modern loyalty programs (poker VIP "missions" systems) increasingly reward *trying something different*, not just volume — e.g., a one-time bonus (loyalty points, never cash) the first time a player enters a tournament in a given week or a format they haven't played before. Cheap to build (a `first_tournament_entry_at` style check against `tournament_entries`), directly targets a ranked-only player's very first tournament entry, which is the highest-leverage single conversion in this whole funnel.

### 5.7 Scheduled tournaments create urgency ranked structurally cannot

Ranked's whole value proposition is "play anytime, no waiting" — which means it can never create appointment-viewing urgency. Tournaments can: `scheduling.ts` already has `RegistrationWindow`/`commitField`. Make sure the UI surfaces a live countdown ("Starts in 8 minutes — 14 seats left") prominently wherever tournaments are listed. This is standard, well-documented DFS/poker MTT scheduling practice and it's a lever ranked cannot use by its own design, so it should be leaned on hard.

### 5.8 Wagers sit between ranked and tournament, deliberately — fee-tier reasoning (2026-08-05)

Phase 4 added a third contest type: equal-stake 1v1 wagers, either posted to an open board or sent to a friend. This is closer to ranked than to a tournament structurally (one opponent, one match, no bracket) but the fee tier landed **above both**: 12% standard / 11% established / 6% elite, charged at the winner's tier — versus ranked's 10%/9%/5% and tournament rates that range 11%–15% (standard) down to 5.5%–7.5% (elite) depending on format (§3, Phase 2).

**Why wagers rake higher than ranked at every tier:** a wager is opt-in, self-selected, player-vs-player-at-a-price contest — the platform is doing less matchmaking work than ranked's Elo-proximity queue does, but it's also a lower-volume, higher-friction product (post-and-wait or challenge-and-wait, versus ranked's near-instant queue), so it doesn't get ranked's low-friction-loss-leader discount. It also sits outside the Elo ladder entirely (no rating moves — §3's Phase 4 entry), so it can't lean on "long-run skill signal" the way ranked's low rake is partly justified by. A rake between ranked and the tournament range keeps the ordering the funnel strategy (§5's opening framing) depends on intact: **ranked stays the cheapest, most frictionless front door; tournaments stay the most profitable, most marketable "event" surface; wagers are a distinct, opt-in social/friend-vs-friend lane that is not cheaper than either of the other two**, so nothing about adding it undercuts the pull toward tournaments the rest of §5 is built to create.

**Why it doesn't get its own §5.x funnel-mechanics section (loyalty points, satellites, etc.):** those mechanisms are specifically about pulling a ranked-only player toward tournaments. A wager is neither the ranked queue nor a tournament — it's driven by a specific social relationship (an open board post, or a named friend) rather than by matchmaking or a scheduled field, so the funnel logic above doesn't directly apply to it. If wagers turn out to meaningfully divert attention from tournaments once real usage data exists, that's a real, separate question worth its own review — not addressed here because there's no data yet to make that call responsibly (same "get the number first" standard applied to the game-balance instrumentation work earlier this handoff).

### What not to do here

Don't raise the tournament rake further to "make ranked look cheap by comparison" — 10%+ is already at the higher end of normal for the category (researched: poker tournament rake typically runs 5–15%, cash games 2.5–10%; above ~15–20% players correctly perceive it as predatory and avoid it), and ranked already sits at that same 10% ceiling for a standard account. Don't raise ranked further either — `BRAND.md`'s entire positioning is now built on the printed 10%/5% pair, and it's already the platform's competitive edge within the category. The lever here is *pulling toward tournaments through structure* (loyalty, satellites, titles, urgency, stake ceilings) since the two products' entry-level rates no longer do that work on their own.

---

## 6. Constraints

- Built for **~5,000 users**, not 5 million. Don't introduce infrastructure (Redis clusters, message queues, multi-region anything) unless a task above genuinely can't be done without it — and even then, prefer the smallest real solution (e.g., a single Redis instance for rate limiting, not a cluster).
- This is a real-money product in an area (skill-gaming/sweepstakes law) the person has already been told needs a lawyer before launch. Don't touch `jurisdiction_rules` data, self-exclusion mechanics, or age-verification logic as if you're making a legal determination — implement what's asked precisely, and if a task implies a legal judgment call, say so instead of guessing.
- Preserve every existing security/idempotency pattern exactly. If you're not sure whether a new function needs `security definer` + `revoke execute`, or an idempotency key, the answer is yes if it touches money, a bracket, or an account-status flag.
- Keep `brand/BRAND.md`'s voice rules on anything user-facing. The banned-vocabulary list ("luck," "chance," "jackpot," "bet"/"wager"/"gamble" as verbs, "win big," "guaranteed," "risk-free") is described in that doc as "legally load-bearing" — treat it that way.

## 7. Definition of done

For every item you complete: `npx tsc --noEmit` clean, `npx next lint` clean, and — for anything that moves money or advances a bracket — an actual executed test (a self-test function in the migration, following the existing `assert_settlement_works()` pattern, or a manual run against a real Supabase project) proving it works, not just that it compiles. Update this file's §3/§4 as you go so the next person (human or agent) inherits an accurate picture instead of a stale one.
